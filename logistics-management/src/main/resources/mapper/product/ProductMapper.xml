<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.buildenough.logistics.product.ProductMapper">

    <select id="findAll" resultType="com.buildenough.logistics.product.ProductDto">
        SELECT
            product_id AS productId,
            product_code AS productCode,
            product_name AS productName,
            specification,
            unit,
            price,
            use_yn AS useYn,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM products
        ORDER BY product_id DESC
    </select>

    <select id="findById" parameterType="int" resultType="com.buildenough.logistics.product.ProductDto">
        SELECT
            product_id AS productId,
            product_code AS productCode,
            product_name AS productName,
            specification,
            unit,
            price,
            use_yn AS useYn,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM products
        WHERE product_id = #{productId}
    </select>

    <insert id="insertProduct"
            parameterType="com.buildenough.logistics.product.ProductCreateRequest"
            useGeneratedKeys="true"
            keyProperty="productId">
        INSERT INTO products (
            product_code,
            product_name,
            specification,
            unit,
            price,
            use_yn
        ) VALUES (
                     #{productCode},
                     #{productName},
                     #{specification},
                     #{unit},
                     #{price},
                     #{useYn}
                 )
    </insert>

    <update id="updateProduct">
        UPDATE products
        SET
            product_name = #{request.productName},
            specification = #{request.specification},
            unit = #{request.unit},
            price = #{request.price},
            use_yn = #{request.useYn}
        WHERE product_id = #{productId}
    </update>

    <update id="deactivateProduct">
        UPDATE products
        SET
            use_yn = 'N'
        WHERE product_id = #{productId}
    </update>

</mapper>